package scnnr

// ParWalk is a concurrent/parallel directory walker
func ParWalk(directory string) ([]string, []string, error) {
	var goodFiles []string
	var badFiles []string

	return goodFiles, badFiles, nil
}
